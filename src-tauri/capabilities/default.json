{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC (events for open-trace) + native file dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
